请根据后台admin模块的页面功能，帮我完善E:\web\crm.laikephp.com\crm_uniapp手机端应用。后台API接口位于api模块，需要确保手机端与后台功能保持一致。
目前已完成的功能：
- 客户列表页面：/pages/client/index
- 客户新增页面：/pages/client/clientSet/clientSet?type=add
需要完善的其他功能模块包括但不限于：
1. 客户详情页面（对应客户列表项点击进入）
2. 客户编辑功能（对应客户详情的编辑操作）
3. 联系人管理页面
4. 商机管理页面（参考商机相关后台功能）
5. 合同管理页面（参考合同相关后台功能）
6. 订单管理页面（参考订单相关后台功能）
7. 跟进记录页面（参考跟进记录相关后台功能）
8. 其他CRM核心功能页面
要求：
- 保持与后台admin模块功能的一致性
- 使用相同的API接口规范
- 保持uniapp前端架构的一致性
- 参考现有已完成页面的代码风格和实现方式
- 确保页面交互体验符合移动端特点
列表展示字段也需要和后台一样,如果字段动态获取,也需要实现动态获取包括表单的新增和编辑,这可以参考admin下的模板文件E:\web\crm.laikephp.com\app\admin\view
``` <div class="layui-form-item">
            <label class="layui-form-label">{:fy('amount')}</label>

            <div class="layui-input-block">

                <input type="text" id="money" name="money"  placeholder="{:fy('Please enter')}{:fy('amount')}" class="layui-input">

            </div>

        </div>```上面这个就是表单固定死的字段,``` {$fields_str|raw}```自定义新增的字段,crm_uniapp手机端也需要可以动态调用出展示

列表字段和字段功能参考E:\web\crm.laikephp.com\public\static\admin\js文件

```   {field: 'id', title: 'id',width:60},

                    {field: 'type', title: '类型',width:150,search: 'select', selectList: CONFIG.typeList},

                    {field: 'email', title: '收件箱',width:150,search: true},

                    {field: 'title', title: '发送标题',search: true},

                   /* {field: 'content', title: '发送内容',search: true},*/

                    {field: 'create_username', title: '操作者',width:100,search: true},

                    {field: 'status', title: '状态','width': 80,templet:function (res) {

                           if(res.status=='success'){

                                return '<span class="green">'+CONFIG.statusList[res.status]+'</span>';

                            }else{

                               return '<span class="red">'+CONFIG.statusList[res.status]+'</span>';

                            }



                        }

                        ,search: 'select', selectList: CONFIG.statusList ,width:100 },

                    {field: 'notes', title: '备注',width:120},

                    {field: 'create_time', title: '发送时间',width:150},

                    {width: 150, title: '操作', templet: ea.table.tool,operat: [ [{

                            class: 'layui-btn layui-btn-xs',

                            method: 'open',

                            text: fy('详情'),

                            auth: 'edit',

                            url: init.edit_url,

                            extend: '', icon: ''

                        }],'delete']}``` 上面的代码就包含了列表的字段和操作按钮,

根据上面的要求请先完善pages/member/list管理员列表渲染功能,我们一步一步确认操作,怕你改完都不能用,而且浪费时间